const express = require('express');
const router = express.Router();

// const { DataProducts, DataClients } = require('../data');
// const data = [];
// DataProducts.input.map((d) => {
//   data.push({ [d.name]: d.title });
// });
// DataProducts.dropdown.map((d) => {
//   data.push({ [d.name]: d.title });
// });
// DataClients.input.map((d) => {
//   data.push({ [d.name]: d.title });
// });
// DataClients.dropdown.map((d) => {
//   data.push({ [d.name]: d.title });
// });

// router.get('/', (req, res) => {
//   res.send(data);
// });

module.exports = router;
